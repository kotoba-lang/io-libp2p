{;; nbb bundles promesa.core (see test/kotoba/net/transport/tcp_demo.cljk)
 ;; but this repo does not declare it as an mvn dependency, so clj-kondo
 ;; can't analyze its macros from source. p/let expands like
 ;; clojure.core/let (sequential bindings, body); tell clj-kondo so it
 ;; doesn't flag every binding introduced inside it as an unresolved
 ;; symbol. Mirrors kotoba-lang/dtn's identical .clj-kondo/config.edn.
 :lint-as {promesa.core/let clojure.core/let}}
